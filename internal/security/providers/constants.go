package providers

const (
	// DefaultAPIKeyQueryParam is the default query parameter name for API key auth.
	DefaultAPIKeyQueryParam = "api_key"
	// BearerTokenType is the OAuth2/Bearer token type prefix.
	BearerTokenType = "Bearer"
)
